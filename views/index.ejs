<!doctype html>
<html lann="en">
    <head>
        <title>Image Search Abstraction Layer</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" type="text/css" />
    </head>
    <body>
        <div class="container">
            <% if (locals.error) { %>
                <h2 style="color: red;"><%= error %></h2>
            <% } %>
            <h1 class="header">
                FreeCodeCamp API Basejump: Image Search Abstraction Layer
            </h1>
            <p>By Kashif Jamal Soofi | <a href="https://github.com/kashifsoofi/fcc-image-search-service">GitHub Project</a> | <a href="swagger">Swagger</a></p>
            <blockquote>
                User stories:
                <ol>
                    <li>I can get the image URLs, alt text and page urls for a set of images relating to a given search string.</li>
                    <li>I can paginate through the responses by adding a ?offset=2 parameter to the URL.</li>
                    <li>I can get a list of the most recently submitted search strings.</li>
                </ol>
            </blockquote>
            <p>
                Image search has been implemented by using <a href="https://www.qwant.com/" target="_blank">Qwant</a>' API.
            </p>
        <h3>Usage:</h3>
            <code><%= appUrl %>search/{term}</code><br/>
            <code><%= appUrl %>search/{term}?offset={offset}</code><br/>
            <code><%= appUrl %>latest/imagesearch</code><br/>
        </div>
    </body>
</html>